 const box = document.querySelector('.pjs'); const processor = document.querySelector('.p'); const inputs = [ processor.querySelector('.c') ]; const outputs = [ processor.querySelector('.r'), processor.querySelector('.d') ]; const label = processor.querySelector('span'); function createCenter() { const element = document.createElement('div'); const screen = {x: window.innerWidth, y: window.innerHeight}; const offset = processor.offsetHeight / 2 + 4; const y = `${(inputs[0].offsetTop + inputs[0].offsetHeight / 2 + 1) - offset - 1.5}px`; element.id = 'abc'; element.style.backgroundColor = 'white'; element.style.translate = `-${screen.x / 4}px ${y}`; box.appendChild(element); setTimeout(()=>{ element.style.translate = `${screen.x / 4}px ${y}`}, 100); const span = document.createElement('span'); span.innerText = 'file'; span.style.opacity = '0'; element.appendChild(span); setTimeout(()=>{ span.style.opacity = '1'},1000); setTimeout(()=>{ span.style.opacity = '0'},3000); setTimeout(()=>{ element.style.translate = `${screen.x / 4 * 3}px ${y}`},3200); setTimeout(()=>{ element.remove()},3450)} 